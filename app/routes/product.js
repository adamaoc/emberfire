import Ember from 'ember';

export default Ember.Route.extend({
	// Should have to have something like this...
	// maybe something else like Firebase is taking care of it instead...
	// model: function(params) {
	// 	return product.findBy('id', params.product_id);
	// }
});
